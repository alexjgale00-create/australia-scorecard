// Phase A stub. The real pipeline (fetching from World Bank / OECD / IMF /
// manual-source folders, normalising, and writing /data/processed/*.json)
// is built in Phase B. This stub exists so `npm run pipeline` already works
// as documented in the README, and fails loudly instead of silently.
console.log("");
console.log("The data pipeline hasn't been built yet — that's Phase B.");
console.log("Right now the site runs entirely on hand-written sample data");
console.log("in /data/processed/, each file clearly marked SAMPLE_DATA.");
console.log("");
console.log("Nothing to do here yet. No files were changed.");
console.log("");
process.exit(0);
